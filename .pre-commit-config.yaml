repos:
  - repo: https://github.com/ansible/ansible-lint
    rev: v25.5.0  # Use the latest stable version or pin as needed
    hooks:
      - id: ansible-lint
        name: ansible-lint
        entry: ansible-lint
        language: python
        types: [yaml]
        files: ^playbooks/|^roles/|\.yml$
