---
# ansible-task
# Quick test mode optimizations
- name: Check if running in optimized base image
  ansible.builtin.stat:
    path: /opt/ansible-home-optimized-base
  register: localhost_optimized_base_check
- name: Ensure ~/code directory exists
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/code"
    state: directory
    mode: "0755"

- name: Configure git user name
  community.general.git_config:
    name: user.name
    value: David Snider
    scope: global

- name: Configure git user email
  community.general.git_config:
    name: user.email
    value: david@davidsnider.org
    scope: global

- name: Configure git to use ssh.exe for SSH connections
  community.general.git_config:
    name: core.sshCommand
    value: ssh.exe
    scope: global
  when: "'microsoft' in ansible_kernel"

- name: Configure git signing key
  community.general.git_config:
    name: user.signingkey
    value: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKWm36FodEyxXOXxqhkCj0YLDHkori4Dzmq3hI0PsrX9
    scope: global

- name: Configure GPG format
  community.general.git_config:
    name: gpg.format
    value: ssh
    scope: global

- name: Configure GPG SSH program
  community.general.git_config:
    name: gpg.ssh.program
    value: /mnt/c/Users/david/AppData/Local/1Password/app/8/op-ssh-sign-wsl
    scope: global

- name: Enable commit GPG signing
  community.general.git_config:
    name: commit.gpgsign
    value: "true"
    scope: global

- name: Ensure .ssh directory exists
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.ssh"
    state: directory
    mode: "0700"

- name: Create SSH allowed signers file
  ansible.builtin.copy:
    content: david@davidsnider.org ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKWm36FodEyxXOXxqhkCj0YLDHkori4Dzmq3hI0PsrX9
    dest: "{{ ansible_env.HOME }}/.ssh/allowed_signers"
    mode: "0644"

- name: Configure Git allowed signers file
  community.general.git_config:
    name: gpg.ssh.allowedSignersFile
    value: "{{ ansible_env.HOME }}/.ssh/allowed_signers"
    scope: global

# Enhanced Git Security Configuration
- name: Require all commits to be signed
  community.general.git_config:
    name: commit.gpgsign
    value: "true"
    scope: global

- name: Require all tags to be signed
  community.general.git_config:
    name: tag.gpgsign
    value: "true"
    scope: global

- name: Enable strict SSL verification
  community.general.git_config:
    name: http.sslverify
    value: "true"
    scope: global

- name: Enable push signature verification
  community.general.git_config:
    name: receive.fsckobjects
    value: "true"
    scope: global

- name: Enable transfer signature verification
  community.general.git_config:
    name: transfer.fsckobjects
    value: "true"
    scope: global

- name: Enable fetch signature verification
  community.general.git_config:
    name: fetch.fsckobjects
    value: "true"
    scope: global

- name: Set minimum TLS version for HTTPS
  community.general.git_config:
    name: http.sslversion
    value: tlsv1.2
    scope: global

- name: Configure SSH key validation
  community.general.git_config:
    name: gpg.ssh.defaultKeyCommand
    value: ssh-add -L
    scope: global

# SSH Key Management and Validation
- name: Validate SSH directory permissions
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.ssh"
    state: directory
    mode: "0700"
    owner: "{{ ansible_user_id }}"

- name: Validate allowed_signers file permissions
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.ssh/allowed_signers"
    mode: "0644"
    owner: "{{ ansible_user_id }}"

- name: Create SSH config for enhanced security
  ansible.builtin.copy:
    dest: "{{ ansible_env.HOME }}/.ssh/config"
    mode: "0600"
    content: |
      # Enhanced SSH Security Configuration
      Host *
        # Use only the configured keys
        IdentitiesOnly yes

        # Disable agent forwarding for security
        ForwardAgent no

        # Disable X11 forwarding for security
        ForwardX11 no

        # Use strong ciphers only
        Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com,aes256-ctr,aes192-ctr,aes128-ctr

        # Use strong MACs only
        MACs hmac-sha2-256-etm@openssh.com,hmac-sha2-512-etm@openssh.com,hmac-sha2-256,hmac-sha2-512

        # Use strong key exchange algorithms only
        KexAlgorithms curve25519-sha256@libssh.org,diffie-hellman-group16-sha512,diffie-hellman-group18-sha512

        # Use strong host key algorithms only
        HostKeyAlgorithms ssh-ed25519-cert-v01@openssh.com,ssh-rsa-cert-v01@openssh.com,ssh-ed25519,rsa-sha2-512,rsa-sha2-256

        # Verify host keys strictly
        StrictHostKeyChecking ask

        # Connection timeout and keep-alive settings
        ConnectTimeout 30
        ServerAliveInterval 60
        ServerAliveCountMax 3

- name: Ensure ~/.local/bin directory exists (early)
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.local/bin"
    state: directory
    mode: "0755"

- name: Create GPG key rotation script
  ansible.builtin.copy:
    dest: "{{ ansible_env.HOME }}/.local/bin/rotate-git-keys"
    mode: "0755"
    content: |
      #!/bin/bash
      # Git SSH Key Rotation Script
      # This script helps rotate SSH keys used for Git signing

      set -euo pipefail

      SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
      SSH_DIR="$HOME/.ssh"
      ALLOWED_SIGNERS="$SSH_DIR/allowed_signers"
      BACKUP_DIR="$SSH_DIR/backups/$(date +%Y%m%d_%H%M%S)"

      echo "🔐 Git SSH Key Rotation Script"
      echo "=============================="

      # Create backup directory
      mkdir -p "$BACKUP_DIR"

      # Backup current allowed_signers
      if [[ -f "$ALLOWED_SIGNERS" ]]; then
          cp "$ALLOWED_SIGNERS" "$BACKUP_DIR/allowed_signers.backup"
          echo "✅ Backed up current allowed_signers to $BACKUP_DIR"
      fi

      echo ""
      echo "Current signing keys in allowed_signers:"
      if [[ -f "$ALLOWED_SIGNERS" ]]; then
          cat "$ALLOWED_SIGNERS" | nl
      else
          echo "No allowed_signers file found"
      fi

      echo ""
      echo "Available SSH keys in agent:"
      if command -v ssh-add >/dev/null 2>&1; then
          ssh-add -L 2>/dev/null || echo "No keys in SSH agent"
      else
          echo "ssh-add not available"
      fi

      echo ""
      echo "To add a new key to allowed_signers:"
      echo "1. Ensure your new key is loaded in 1Password SSH agent"
      echo "2. Run: echo 'david@davidsnider.org <new-key-type> <new-public-key>' >> ~/.ssh/allowed_signers"
      echo "3. Update Git configuration: git config --global user.signingkey '<new-public-key>'"
      echo ""
      echo "⚠️  Remember to update the Ansible configuration in roles/localhost/tasks/local-linux.yml"
      echo "    with the new signing key for consistent deployment."

      echo ""
      echo "🔍 Verifying current Git configuration..."
      echo "Current signing key: $(git config --global user.signingkey || echo 'Not set')"
      echo "GPG format: $(git config --global gpg.format || echo 'Not set')"
      echo "Commit signing enabled: $(git config --global commit.gpgsign || echo 'Not set')"

- name: Install a package and update cache if needed
  become: true
  ansible.builtin.apt:
    name:
      - gh
      - htop
      - jq
      - python3-poetry
      - pre-commit
      - zsh
      # - zsh-autocomplete
      - zsh-autosuggestions
      - zsh-syntax-highlighting
      - unzip
    state: present
    update_cache: true
    cache_valid_time: 86400 # in seconds, equals 24 hours
  when: not localhost_optimized_base_check.stat.exists
  retries: 3
  delay: 5
  timeout: 300

- name: Verify apt packages installation
  ansible.builtin.command: gh --version
  register: localhost_gh_version_check_linux
  changed_when: false
  failed_when: localhost_gh_version_check_linux.rc != 0

- name: Debug apt packages installation check
  ansible.builtin.debug:
    var: localhost_gh_version_check_linux
    verbosity: 2

- name: Get current user
  ansible.builtin.command: whoami
  register: localhost_current_user
  changed_when: false

- name: Debug current user
  ansible.builtin.debug:
    var: localhost_current_user
    verbosity: 2

- name: Set zsh as default shell
  ansible.builtin.user:
    name: "{{ localhost_current_user.stdout }}"
    shell: /usr/bin/zsh
  become: true

- name: Ensure ~/.local/bin directory exists
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.local/bin"
    state: directory
    mode: "0755"

- name: Download oh-my-posh installation script
  ansible.builtin.get_url:
    url: https://ohmyposh.dev/install.sh
    dest: /tmp/oh-my-posh-install.sh
    mode: "0755"
  retries: 3
  delay: 5
  timeout: 300

- name: Install oh-my-posh if not present
  ansible.builtin.shell:
    cmd: zsh /tmp/oh-my-posh-install.sh
    executable: /usr/bin/zsh
  environment:
    OMP_INSTALL_DIR: "{{ ansible_env.HOME }}/.local/bin"
  args:
    creates: "{{ ansible_env.HOME }}/.local/bin/oh-my-posh"
  when: not (ansible_facts.virtualization_type is defined and ansible_facts.virtualization_type == 'docker')
  failed_when: false

- name: Check if Oh My Zsh is installed
  ansible.builtin.stat:
    path: "{{ ansible_env.HOME }}/.oh-my-zsh"
  register: localhost_oh_my_zsh_stat

- name: Debug Oh My Zsh stat
  ansible.builtin.debug:
    var: localhost_oh_my_zsh_stat
    verbosity: 2

- name: Install Oh My Zsh with error handling
  block:
    - name: Install Oh My Zsh if not present
      ansible.builtin.shell: |
        sh -c "$(curl -fsSL -m 300 https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended
      args:
        creates: "{{ ansible_env.HOME }}/.oh-my-zsh"
      when: not localhost_oh_my_zsh_stat.stat.exists
      changed_when: false
      register: localhost_oh_my_zsh_install_result
      retries: 3
      delay: 5
      until: localhost_oh_my_zsh_install_result.rc == 0

    - name: Verify Oh My Zsh installation
      ansible.builtin.stat:
        path: "{{ ansible_env.HOME }}/.oh-my-zsh"
      register: localhost_oh_my_zsh_stat_check_linux

    - name: Fail if Oh My Zsh is not installed
      ansible.builtin.fail:
        msg: "Oh My Zsh installation failed. The directory ~/.oh-my-zsh was not found."
      when: not localhost_oh_my_zsh_stat_check_linux.stat.exists
  rescue:
    - name: Fail if Oh My Zsh installation failed
      ansible.builtin.fail:
        msg: "Oh My Zsh installation failed. Please check the logs for more details."

- name: Debug Oh My Zsh installation result
  ansible.builtin.debug:
    var: localhost_oh_my_zsh_install_result
    verbosity: 2

- name: Verify Oh My Zsh installation
  ansible.builtin.stat:
    path: "{{ ansible_env.HOME }}/.oh-my-zsh"
  register: localhost_oh_my_zsh_stat_check_linux

- name: Debug Oh My Zsh installation check
  ansible.builtin.debug:
    var: localhost_oh_my_zsh_stat_check_linux
    verbosity: 2

- name: Fail if Oh My Zsh is not installed
  ansible.builtin.fail:
    msg: "Oh My Zsh installation failed. The directory ~/.oh-my-zsh was not found."
  when: not localhost_oh_my_zsh_stat_check_linux.stat.exists

- name: Copy zshrc file
  ansible.builtin.copy:
    src: zshrc-linux
    dest: "{{ ansible_env.HOME }}/.zshrc"
    mode: "0644"
  changed_when: false

- name: Copy zprofile file
  ansible.builtin.copy:
    src: zprofile-linux
    dest: "{{ ansible_env.HOME }}/.zprofile"
    mode: "0644"
    backup: true

- name: Install Pulumi CLI
  block:
    - name: Check if Pulumi is already installed
      ansible.builtin.command: which pulumi
      register: localhost_pulumi_check
      failed_when: false
      changed_when: false

    - name: Debug Pulumi check
      ansible.builtin.debug:
        var: localhost_pulumi_check
        verbosity: 2

    - name: Download Pulumi installation script
      ansible.builtin.get_url:
        url: https://get.pulumi.com
        dest: /tmp/pulumi-install.sh
        mode: "0755"
      when: localhost_pulumi_check.rc != 0 and not localhost_optimized_base_check.stat.exists
      retries: 3
      delay: 5
      timeout: 300

    - name: Install Pulumi CLI
      ansible.builtin.shell:
        cmd: bash /tmp/pulumi-install.sh
        executable: /bin/bash
      when: localhost_pulumi_check.rc != 0 and not localhost_optimized_base_check.stat.exists
      register: localhost_pulumi_install_result
      changed_when: false

    - name: Verify Pulumi installation
      ansible.builtin.command: pulumi version
      register: localhost_pulumi_version_check
      changed_when: false
      failed_when: localhost_pulumi_version_check.rc != 0

    - name: Debug Pulumi installation result
      ansible.builtin.debug:
        var: localhost_pulumi_install_result
        verbosity: 2

    - name: Debug Pulumi version check
      ansible.builtin.debug:
        var: localhost_pulumi_version_check
        verbosity: 2
  rescue:
    - name: Fail if Pulumi installation failed
      ansible.builtin.fail:
        msg: "Pulumi installation failed. Please check the logs for more details."

- name: Download and install 1Password CLI
  when: not localhost_optimized_base_check.stat.exists
  block:
    - name: Check if GPG keyring already exists
      ansible.builtin.stat:
        path: /usr/share/keyrings/1password-archive-keyring.gpg
      register: localhost_gpg_keyring_exists

    - name: Download 1Password GPG key
      ansible.builtin.get_url:
        url: https://downloads.1password.com/linux/keys/1password.asc
        dest: /tmp/1password.asc
        mode: "0644"
        force: false
      register: localhost_gpg_key_download
      when: not localhost_gpg_keyring_exists.stat.exists
      changed_when: false
      retries: 3
      delay: 5
      timeout: 300

    - name: Create 1Password keyring directory
      ansible.builtin.file:
        path: /usr/share/keyrings
        state: directory
        mode: "0755"
      become: true

    - name: Import 1Password GPG key to system keyring
      ansible.builtin.shell: |
        gpg --dearmor < /tmp/1password.asc > /usr/share/keyrings/1password-archive-keyring.gpg
      become: true
      when: not localhost_gpg_keyring_exists.stat.exists
      args:
        creates: /usr/share/keyrings/1password-archive-keyring.gpg
      register: localhost_gpg_import_result
      changed_when: false

    - name: Validate GPG keyring file permissions
      ansible.builtin.file:
        path: /usr/share/keyrings/1password-archive-keyring.gpg
        mode: "0644"
        owner: root
        group: root
      become: true

    - name: Get system architecture
      ansible.builtin.command: dpkg --print-architecture
      register: localhost_system_arch
      changed_when: false

    - name: Add 1Password repository
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch={{ localhost_system_arch.stdout }}
          signed-by=/usr/share/keyrings/1password-archive-keyring.gpg]
          https://downloads.1password.com/linux/debian/{{ localhost_system_arch.stdout }}
          stable main
        filename: 1password
        state: present
      become: true
      retries: 3
      delay: 5

    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
      become: true
      retries: 3
      delay: 5

    - name: Install 1Password CLI
      ansible.builtin.apt:
        name: 1password-cli
        state: present
      become: true
      retries: 3
      delay: 5

    - name: Verify 1Password CLI installation and version
      ansible.builtin.command: op --version
      register: localhost_op_version_linux
      changed_when: false

    - name: Validate 1Password CLI version meets security requirements
      ansible.builtin.fail:
        msg: "1Password CLI version {{ localhost_op_version_linux.stdout }} does not meet minimum security requirements (2.0.0+)"
      when:
        - localhost_op_version_linux.stdout is defined
        - (localhost_op_version_linux.stdout | regex_search('([0-9]+)\\.([0-9]+)\\.([0-9]+)') | split('.') | first | int) < 2
  rescue:
    - name: Fail if 1Password CLI installation failed
      ansible.builtin.fail:
        msg: "1Password CLI installation failed. Please check the logs for more details."

  always:
    - name: Clean up temporary files
      ansible.builtin.file:
        path: /tmp/1password.asc
        state: absent
      register: localhost_cleanup_result
      changed_when: false

# 1Password CLI Security Enhancements
- name: Include 1Password security enhancements
  ansible.builtin.include_tasks: 1password-security.yml

- name: Run 1Password CLI health check
  ansible.builtin.command: "{{ ansible_env.HOME }}/.local/bin/op-health-check"
  register: localhost_op_health_check_linux
  changed_when: false
  failed_when: localhost_op_health_check_linux.rc > 1  # Allow warnings (rc=1) but fail on errors (rc=2)
