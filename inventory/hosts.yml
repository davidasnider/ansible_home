all:
  children:
    workstations:
      children:
        local_workstations:
          hosts:
            localhost:
              ansible_connection: local
              ansible_python_interpreter: /usr/bin/python3
        networked_workstations:
          hosts:
            # Example networked workstation
            # workstation1:
            #   ansible_host: 192.168.1.50
    raspberry_pis:
      hosts:
        # Example placeholders for always-on Raspberry Pis
        # pi1:
        #   ansible_host: 192.168.1.100
        #   ansible_user: pi
